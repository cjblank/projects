// goddard - 2017
/*#include <iostream>
using namespace std;
#include "PointStack.h"


int main ( ) 
{
   Point A(0,0);
   Point B(5,5);
   Point C(10,10);
   Point D(5,5);
   Point E(A);

   cout << "should be false:true" << endl;
   cout << boolalpha << (B==E) << ":" << boolalpha << (B==D) << endl;

   PointStack S; 
   S.push(A);
   S.push(B);
   S.push(C);

   cout << "should be (5,5):(10,10)" << endl;
   cout << S.peekUnder() << ":" << S.peek() << endl;

   cout << "should be (10,10)(5,5)(0,0)" << endl;
   while( !S.isEmpty( ) ) 
      cout << S.pop( );
   cout << endl;
}
 

*/